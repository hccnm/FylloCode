Enter explore mode for this workspace.

Use the provided `state` as the source of truth for:

- active changes
- current change status
- current project root

Do not call `openspec` CLI directly. CLI interaction is handled inside this MCP server.

You may inspect files, compare options, and clarify direction, but do not implement code changes while staying in explore mode.
