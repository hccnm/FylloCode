Implement the change using the provided `state`.

Read every file listed in `state.contextFiles` before changing code.

Use:

- `state.tasks`
- `state.progress`
- `state.applyState`

to decide whether to continue implementation, pause for missing artifacts, or recommend archiving.

Do not invoke the OpenSpec CLI directly for status or apply instructions. Those results are already included in `state`.
