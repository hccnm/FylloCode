import { DEFAULT_ACP_AGENT_ID } from "@shared/constants/agents";

// Future compatible agents must be added to this list deliberately.
export const CLAUDE_CODE_AGENT_IDS: readonly string[] = [DEFAULT_ACP_AGENT_ID];
